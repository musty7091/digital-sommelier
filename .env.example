# Frontend ortam degiskenleri (opsiyonel).
# Uygulama LOKAL calisir; varsayilan olarak yerel API'leri kullanir, bu dosya zorunlu degildir.
# Yalnizca veritabani API'sini farkli bir adreste calistiriyorsan asagidakini ayarla:
# VITE_PRODUCT_DB_API_URL=http://localhost:8788

# DB baglanti ayarlari ICIN bu dosya DEGIL, server/.env kullanilir (server/.env.example'a bak).
